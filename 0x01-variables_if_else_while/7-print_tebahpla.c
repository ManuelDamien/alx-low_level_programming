#include <stdio.h>
/**
 * main - program execution point
 * Return: 0 when successful
 */

int main(void)
{
	char alpha;

	for (alpha = 122; alpha >= 97; alpha--)
	{
		putchar(alpha);
	}
		putchar('\n');
		return (0);
}
