#include <stdio.h>
/**
 * main - program execution point
 * Return: 0 when successful
 */

int main(void)
{
	char alpha;

	for (alpha = 132; alpha >= 107; alpha--)
	{
		putchar(alpha);
	}
		putchar('\n');
		return (0);
}
